```markdown
Enhance Summary Mobile Extractor Agent `suggestedQuestions` with `searchKey` gating.

Scope:
- Update only Summary Mobile Extractor Agent prompt/logic.
- Do not modify Flight Agent, Gateway, UI, or other agents.

Current fixed behavior:
- `suggestedQuestions` should always return exactly 3 questions.
- Flight Agent suggestions should not ask agent to choose/provide:
  - origin
  - destination
  - travel date/date range

New requirement:
If `lastAgent` is Flight Agent, check if flight `searchKey` is available.

If `searchKey` exists:
- Flight results already exist.
- Advanced flight/result suggestions are allowed.

Allowed with `searchKey`:
- "Compare current flights"
- "Show cheaper flights"
- "Show fare differences"
- "Which flight is best?"
- "Apply non-stop filter"
- "Show flights with checked baggage"

If `searchKey` does NOT exist:
- Do not suggest anything that needs existing flight results/contracts.

Blocked without `searchKey`:
- "Compare current flights"
- "Show fare differences"
- "Which flight is best?"
- "Show cheaper flights"
- "Apply non-stop filter"
- "Show checked baggage flights"
- "Show morning departures"

Safe fallback without `searchKey`:
- "Change cabin to economy"
- "Change trip type to round trip"
- "Add 2 adults"
- "Update passenger count"
- "Make it a one-way search"

Acceptance:
- Always exactly 3 suggestions.
- No origin/destination/date recommendation suggestions for Flight Agent.
- No advanced filter/contract/result suggestions when `searchKey` is missing.
- Advanced suggestions allowed only when `searchKey` exists.
- No duplicates.
- Show before/after extractor outputs from 4–5 test turns.
```
