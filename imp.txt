
```markdown
Fix Policy Agent source URL/citation handling.

Problem:
Policy Agent uses a retrieval tool to fetch policy/airline-related chunks. Sometimes the response cites content like `[1]` or `URL 1`, but the actual URL is missing in the final answer.

Expected behavior:
- If Policy Agent cites a source, it must include the actual URL from the retrieved chunk metadata.
- Never output placeholder text like:
  - `URL 1`
  - `Source 1`
  - `[1]` without the real URL
- If retrieved chunk has no URL, do not create a fake/placeholder URL.
- If multiple chunks are used, map each citation correctly to its source URL.
- Final answer should include clear source references only when valid URLs exist.

Please update:
- Policy Agent prompt/instructions
- Citation/source formatting logic, if needed
- Retrieval chunk parsing if URL metadata is not being passed correctly

Test cases:
1. Retrieved chunk has URL → final response includes actual URL.
2. Multiple chunks have URLs → citations map correctly.
3. Retrieved chunk has no URL → no placeholder URL is shown.
4. Agent should never output `URL 1` without real URL.

Deliverable:
- Fix the issue.
- Add tests.
- Share root cause and before/after examples.
```

