Hi, quick clarification on `IsDisabled`.

For time filters, when `IsDisabled: true`, the option is greyed out in the UI, which makes sense. But I noticed that in some cases, like NYC → LHR, alternate airport options also have `IsDisabled: true`, but they still look selectable/applicable in the UI.

Can you confirm if `IsDisabled` works differently for airport filters compared to time filters? Should we ignore it for airports, or is there some separate logic behind this?

