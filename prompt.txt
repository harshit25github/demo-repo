# Fix Flight Search Priority

Review the Flight Agent prompt priority logic.

Issue: agent is over-prioritizing existing search parameters. Existing search criteria should be a fallback, not override new explicit user input.

Example:

* Existing search: Gurgaon → Goa
* User says: Delhi → Goa
* Expected: use Delhi → Goa

Plan the best fix and update prompt rules so priority becomes:
**current explicit user input > existing search parameters > older history/defaults**

Also mention if any context/routing change is needed beyond prompt updates. Do not implement until the plan is reviewed.
