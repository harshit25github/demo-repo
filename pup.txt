```markdown id="searchcriteria-skip-when-missing"
Update `/chat` searchCriteria handling.

Requirement:
If `searchCriteria` is not present in the incoming `/chat` request payload:

- Do not create/generate `searchCriteria` from live/context data.
- Do not send the final `searchCriteria` chunk.
- Do not fallback to old context just to build `searchCriteria`.

Only create/update/send `searchCriteria` when it exists in the original request payload.

Expected behavior:
- Request has `searchCriteria` → update it and send searchCriteria chunk.
- Request does not have `searchCriteria` → skip searchCriteria creation and skip searchCriteria chunk.

Scope:
- `/chat` request handling
- final context/searchCriteria mapper
- streaming chunk emission

Do not modify unrelated agent/tool logic.

Deliverable:
- Add this conditional handling.
- Share files changed.
- Add test for request with and without `searchCriteria`.
```
