We need the DB team to create a PostgreSQL database on one of the existing production servers. Both our Next.js application (which has server-side API routes) and a separate backend service will connect to this database for reading and writing data.
