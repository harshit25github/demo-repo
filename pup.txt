const text = "$1,000.34 USD";

const match = text.match(/[\$₹€£]?\s?\d{1,3}(?:,\d{3})*(?:\.\d+)?|\d+(\.\d+)?/);

const value = match ? parseFloat(match[0].replace(/[^0-9.]/g, '')) : null;

console.log(value); // 1000.34
