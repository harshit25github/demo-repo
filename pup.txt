Task:
Fix backend update flow for Gateway agent model and agent mapping changes.

Context:
App/agent persistence issue is resolved, but new issues are found.

Current Gateway/Orchestrator should use only these agents:
- Flight Agent
- Policy Agent
- Trip Planner Agent

Issue:
When updating the app/agent setup:
- Prompt updates are working.
- But selected agents are not updating correctly.
- Old/unused agent like Hotel Agent is still showing.
- Model changes are also not getting updated properly.
- Agent-to-app / gateway-to-agent mapping seems stale or incorrect.

Required Work:
Debug and fix the backend update flow so that whenever model or agent selection changes, DB tables are updated correctly.

Please Check:
- App update API flow.
- Gateway/orchestrator update logic.
- Agent-to-app mapping table.
- Gateway-to-agent mapping table.
- Model field update logic.
- Whether old agents are removed/unlinked properly.
- Whether new selected agents are linked properly.
- Whether stale/cache data is being returned.
- Whether response mapper is showing old agent data.

Expected Behavior:
- If selected agents are Flight, Policy, and Trip Planner, only these should appear.
- Hotel Agent should not appear unless explicitly selected.
- Model changes should persist in DB.
- Prompt changes should continue working.
- Agent mappings should reflect the latest selected agents.
- Retrieval API should return updated model + updated agent list.

Acceptance Criteria:
- Updating model updates DB correctly.
- Updating selected agents updates mapping tables correctly.
- Removed agents are unlinked from the app/gateway.
- Retrieval by App ID returns only current agents.
- No stale Hotel Agent appears.
- Add/update tests for model update and agent mapping update.
