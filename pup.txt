Here, “Cheapest flight” is acting as an intent capture, not a complete search request.

The user has given the route/OND, but the travel date is still missing. So even if the user selects “Cheapest flight”, the agent still needs the travel date to perform the actual flight search and return accurate results.

So the agent asking for the travel date is expected behavior. The suggestion helps identify the user’s intent, while the missing mandatory fields are still required to complete the search.
