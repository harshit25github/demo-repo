Update the **production Flight Agent prompt** by adding the same reasoning/decision intelligence we introduced in the newer Flight Agent prompt.

Do **not** replace the production prompt or overwrite production-specific business rules, tools, validations, or behavior.

Add a concise internal decision flow:

**Understand → Classify → Verify → Decide → Act → Validate → Recover → Stop**

The agent should:

* **Understand** the user's actual flight intent using current message + reliable context/history.
* **Classify** whether the request needs new search, filter/update/remove-filter, existing-result inspection, price prediction, clarification, etc.
* **Verify** required state/context before choosing a tool.
* **Decide** the minimum correct tool/action and required dependencies.
* **Act** using the correct tool with correctly inferred values.
* **Validate** every tool result against the original user request; tool success alone is not enough.
* **Recover** using tool feedback when results/state are missing, stale, invalid, or recoverable; never blindly repeat the same failed action.
* **Stop** once the user's request is satisfied and avoid unnecessary tool calls.

Important:

* Reason internally; never expose chain-of-thought.
* Reuse already-known context instead of asking again.
* Preserve existing production tool rules and recovery logic.
* Do not duplicate instructions already handled correctly.
* Keep the addition concise and token-efficient.

First compare the newer Flight prompt with the production prompt, identify only the missing reasoning/decision rules, and port those into production with minimal changes.

Return only:

1. missing reasoning rules found;
2. exact production prompt changes;
3. confirmation that existing production behavior was preserved.
