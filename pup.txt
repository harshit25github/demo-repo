const text = "Total price is 1,200.34 USD";

const match = text.match(/\b\d{1,3}(?:,\d{3})*(?:\.\d+)?|\d+(\.\d+)?\b/);

const value = match ? parseFloat(match[0].replace(/,/g, '')) : null;

console.log(value); // 1200.34
