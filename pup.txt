function compareFloats(a, b) {
  const diff = Math.abs(a - b).toFixed(2);

  if (a === b) {
    return `Both values are equal.`;
  } else if (a < b) {
    return `A (${a}) is less than B (${b}) by ${diff}`;
  } else {
    return `B (${b}) is less than A (${a}) by ${diff}`;
  }
}
