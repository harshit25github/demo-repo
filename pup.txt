You are working on our generative-AI-powered backend, which contains multiple agents. Please inspect the existing agent configuration, prompts, model declarations, guardrails, and any related tests before making changes.

Tasks:

1. Planner Agent
- The planner agent model has already been changed to GPT-5.5.
- Please review the planner agent prompt, schema usage, tool-calling behavior, response format, and any model-specific assumptions.
- If the current prompt/code is already compatible and stable with GPT-5.5, do not make unnecessary changes.
- If changes are required for GPT-5.5 compatibility, make the smallest safe updates and document what was changed.

2. Gateway Agent
- The gateway agent is currently using Sofa 1.1.
- Migrate this agent to GPT-5.4.
- Update the model configuration accordingly.
- Convert/refactor the gateway agent prompt so it is compatible with GPT-5.4.
- Preserve the existing behavior and responsibilities of the gateway agent unless a change is required for compatibility or stability.
- Make sure routing, classification, delegation, and tool-selection behavior remain stable.

3. Unified Input Guardrail Agent
- We recently added a flight agent.
- Some flight-related queries, especially comparison queries, are incorrectly triggering the unified input guardrail agent.
- Please update the unified input guardrail prompt so legitimate flight-related requests are not blocked.
- In particular, flight search, flight comparison, fare comparison, airline comparison, route comparison, booking-preference comparison, and similar travel-planning queries should be treated as valid user intent.
- The guardrail should still block genuinely unsafe, disallowed, abusive, fraudulent, or policy-violating requests.
- Please add or update examples in the prompt so the guardrail clearly understands that normal flight comparison queries are allowed.

4. Post-Booking Guardrail Agent
- The post-booking guardrail agent is currently on GPT-4.1 nano.
- Migrate this agent to GPT-5.4 nano.
- Update the model configuration accordingly.
- Rewrite or adjust the prompt to be stable and understandable for GPT-5.4 nano.
- Keep the prompt concise, explicit, and deterministic because this is a nano model.
- Preserve the current guardrail behavior, but improve clarity and reduce false positives.

General requirements:
- Keep changes minimal and focused.
- Do not rewrite unrelated logic.
- Maintain existing API contracts, response formats, JSON schemas, and tool interfaces.
- Update tests or add targeted regression tests where appropriate.
- Add test cases for flight comparison queries that should not trigger the unified input guardrail.
- Add test cases for unsafe or invalid inputs that should still be blocked.
- After changes, run the relevant test suite and report:
  - files changed,
  - models updated,
  - prompts modified,
  - tests added/updated,
  - any compatibility concerns,
  - and whether any behavior changed intentionally.

Expected outcome:
- Planner agent verified for GPT-5.5 compatibility.
- Gateway agent migrated from Sofa 1.1 to GPT-5.4 with a compatible prompt.
- Unified input guardrail updated so valid flight-agent comparison queries are allowed.
- Post-booking guardrail migrated from GPT-4.1 nano to GPT-5.4 nano with a stable prompt.
- All relevant tests passing.
