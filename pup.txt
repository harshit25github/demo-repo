```markdown
Improve new `price_prediction_tool` response style using old tool as reference.

Scope:
- Compare old price prediction tool and new `price_prediction_tool`.
- Update only the new tool response/formatting layer unless required.
- Do not change unrelated Flight Agent logic.

Issue:
The old price prediction tool returned structured and user-friendly prediction feedback, including:
- clear price buckets
- labels like Great Deal / Fair Price / Premium
- better recommendation text
- organized prediction summaries
- readable output for the agent

The new tool should follow the same quality/style so Flight Agent can give better answers.

Task:
1. Review old price prediction tool response formatting.
2. Review new `price_prediction_tool` output.
3. Bring the new tool output closer to the old tool style.

Expected output improvements:
- Categorize predictions into clear buckets:
  - Great Deal / Low
  - Fair Price / Medium
  - Premium / High
- Include useful labels and summary.
- Include best/cheapest date recommendation.
- Include price range summary if available:
  - lowest
  - highest
  - average
- Include clean formatted response for Flight Agent to consume.
- Keep output structured and consistent.
- Do not expose raw DB/internal data.

Also verify:
- one-way output formatting
- round-trip output formatting
- error/fallback responses
- unsupported/no prediction response

Deliverable:
- Compare old vs new output.
- Update new tool response style.
- Share before/after sample outputs.
- Add/update tests for formatted response, buckets, labels, and recommendation text.
```
