We have added Akamai City as a fallback signal for user widget/origin identification. This value will be used only during new chatId creation and will not be used once the chat is already initiated.

For origin identification, the priority will now be:

Existing priority criteria
User message context
Akamai City as fallback

So, Akamai City will only help when stronger signals are not available during chat initialization.
