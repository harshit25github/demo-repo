# Fix Policy Suggested Question Repetition

Update Policy Helper Agent dynamic prompt to include the existing suggested questions already stored in context.

Goal:

* Agent must know what questions were already generated.
* Do not generate the same or very similar questions again.
* Keep existing static prompt rules as-is unless a small clarification is needed.
* Keep the injected context compact to avoid token increase.

Please inspect the current Policy Agent prompt/context flow and add the minimal dynamic context needed to prevent repeated suggested questions.
