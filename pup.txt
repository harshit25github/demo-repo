I currently have two tools in my agent: one is a "Validate Trip Date" tool, and the other is a "Web Search" tool provided by OpenAI. I want to combine these into a single workflow within the agent. The idea is that if a user asks for date validation, the combined tool will first validate the dates. Then, if the user also needs to fetch any related events for that date, the tool will use a sub-agent.

This sub-agent will run the web search tool, and it will always be required to run if event information is needed. The sub-agent will receive the validated date and figure out the next upcoming occurrence of the event based on the user's current date. For example, if the user mentions "Christmas," the agent will know the current year and will calculate the next upcoming Christmas date. Then it will use the web search tool to fetch events for that date and return the information.

In summary, I need to update the current prompt so that the agent first validates the date and then, if required, uses the sub-agent to find the next occurrence of the event and pull event details. This will ensure that the user gets both date validation and relevant event information in a single streamlined process.
