Hi, I need one clarification regarding the location API flow.

When I search for “Bangkok” using the first API, it returns `BKKG` with type as `group_city`. I then pass this code to the second API to fetch airport details like `airportCode`, `airportCodeText`, `countryCode`, and `airportType`.

But when I pass `BKKG`, the second API returns a different location, like `TBI` / Bahamas-related data, which seems incorrect.

APIs used:

* Location API: `<add API here>`
* Airport details API: `<add API here>`

Could you please confirm if `BKKG` can be directly passed to the second API, or do we need to transform/map `group_city` codes before calling it?
