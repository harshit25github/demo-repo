Refactor my old single-agent TripPlanner into a multi-agent system with the OpenAI Agents SDK (TypeScript).

Read first: prompt.js, multiagent.js, and multiagent2.txt. Do not lose any domain rules, tone, slot semantics, or defaults—carry them over.

Agents to create:

Gateway (orchestrator): Routes via handoffs: Destination → Itinerary → Booking. Keep prompts short; routing rules explicit. 



Destination: Validates/disambiguates destination and returns JSON only (visa, safety, entry steps, best time, health/practical, sources).

Itinerary: Always call Destination as a tool first; then render a “Destination Brief” (from JSON) and a day-wise itinerary (morning/afternoon/evening). 
OpenAI GitHub

Booking: Assumes itinerary finalized; outputs a concise ready-to-book checklist; asks for missing info.

Tooling: Expose Destination as check_destination (Zod-typed params). Itinerary consumes it and renders user-visible details (not a one-liner). 


State (slots): Preserve previous slots (origin, destination, dates → ISO, pax, currency default INR, budget_type/amount, hotel area, destinationVerified, itineraryFinalized). Store in run context. The agent loop should finish only when no pending tools/handoffs remain. 
