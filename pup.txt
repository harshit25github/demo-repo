````markdown id="trip-planner-response-tone-update"
Update Trip Planner Agent prompt based on manager feedback.

Goal:
Incorporate the provided manager suggestions into the current Trip Planner Agent prompt, mainly around:
- output response format
- response length/token control
- tighter response style
- improved tone
- better user-facing quality

## Manager suggestions

Apply these instructions:

```txt
<PASTE_MANAGER_SUGGESTIONS_HERE>
````

## Scope

Update only:

* Trip Planner Agent prompt
* dynamic prompt instructions, if needed
* response formatting rules
* tone/style rules
* output length/token-control instructions

Do not modify:

* unrelated agents
* tool schemas
* routing logic
* guardrails
* image/card rules unless the suggestion explicitly requires it
* web search logic unless the suggestion explicitly requires it

## Requirements

1. Output response format

* Make Trip Planner responses clean, structured, and easy to read.
* Keep responses tight and mobile-friendly.
* Avoid long paragraphs.
* Avoid unnecessary explanations.
* Prioritize actionable trip details.

2. Token/length control

* Add clear prompt instructions to keep responses concise.
* Avoid over-detailed itineraries unless user asks.
* Prefer short day-wise plans, compact bullets, and crisp recommendations.
* Keep the response within the expected token range mentioned in the manager feedback.

3. Tone update

* Update the tone according to the provided suggestion.
* Keep it natural, helpful, confident, and user-friendly.
* Avoid robotic, overly formal, or overly verbose responses.

4. Existing rules must stay intact
   Preserve current Trip Planner rules for:

* no hotels
* no cars
* no commute/local transport
* no raw links
* image/card handling
* web search usage
* suggested questions restrictions
* context-aware responses

5. Conflict check
   Before updating, check if the new suggestions conflict with existing prompt rules.
   If any conflict exists, highlight it and choose the safer production-ready behavior.

## Testing

Add/update tests for:

* normal itinerary generation
* partial context flow
* full context flow
* short user input
* response length control
* tone consistency
* no hotel/car/commute leakage
* no raw links

For each test, capture:

* user input
* context used
* response before
* response after
* token/length estimate
* pass/fail

## Deliverable

* Update the Trip Planner prompt.
* Share files changed.
* Share what instructions were added/modified.
* Share before/after response examples.
* Share test results.
* Mention any prompt conflicts found.

```
```
