```markdown
Verify and complete the new `price_prediction_tool` integration.

Context:
I have updated the Flight Agent prompt and added a new tool named `price_prediction_tool`.

There was already an older price prediction tool in the codebase. The new tool currently exists, but it does not yet have the proper ClickHouse client integration.

Task:
Please compare the old price prediction tool and the new `price_prediction_tool`, then complete the new tool using the old tool as reference.

Check and fix:

1. ClickHouse client
- New `price_prediction_tool` should use the same ClickHouse client/setup pattern as the old tool.
- Reuse existing client/config/helper if available.
- Do not create duplicate connection logic unnecessarily.

2. Input contract
- Compare inputs of old tool vs new tool.
- Verify the Flight Agent is sending the correct payload to the new tool.
- Fix any mismatch in field names, date format, trip type, origin/destination, return dates, etc.

3. Query logic
- Verify the ClickHouse query is built correctly.
- Ensure required filters are used:
  - origin city
  - destination city
  - trip type / round trip flag
  - search date
  - valid prediction window

4. Validation
- Validate required fields.
- Validate date range.
- Ensure tool works only within supported prediction range.
- Return clear structured errors for invalid/missing input.

5. Output contract
- Verify new tool output is clean and usable by Flight Agent.
- Ensure output includes useful prediction result, cheapest/best dates, fallback/error info if needed.
- Do not return raw DB/internal errors to the agent.

6. Flight Agent prompt/tool registration
- Verify new `price_prediction_tool` is properly registered in Flight Agent tools.
- Verify dynamic Flight Agent prompt mentions the new tool correctly.
- Verify old price prediction tool is not accidentally being used if new tool should replace it.

7. Tests
Test these flows:
- valid one-way price prediction
- valid round-trip price prediction
- unsupported route
- missing origin/destination
- invalid date range
- date outside supported range
- flexible/vague date user query triggers price prediction

Deliverable:
- Hydrate new `price_prediction_tool` with ClickHouse client.
- Fix input/output mismatches.
- Verify Flight Agent uses the new tool correctly.
- Share files changed.
- Share before/after sample input/output.
- Share test results.
```
