Update listing API domain and Digital API platform/user-agent config.

Scope:

* Check listing page related files.
* Check listing API/service files.
* Check tools/helpers where listing URLs are used.
* Do not change unrelated APIs.

Required changes:

1. Domain update
   Some previous changes were reverted. Please update listing-related URLs again:

From:
`www.gcode.com`

To:
`m.gcode.com`

Verify no listing-related `www.cheapoair.com` URL remains.

2. Digital API platform/user-agent update
   In the Digital API service where we pass platform/user-agent details, update the values as per below:

```ts
platform: "[ADD_PLATFORM_HERE]" // expected: iOS
userAgent: "[ADD_USER_AGENT_HERE]"
```

I will provide the exact `platform` and `userAgent` values.

Important:

* Keep existing API paths and query params unchanged.
* Only update domain/platform/user-agent where listing/Digital API flow requires it.
* Prefer config/constants if available instead of hardcoding in multiple places.

Deliverable:

* Update required files.
* Share files changed.
* Share old vs new URL/config examples.
* Confirm listing/Digital API flow is using `m.gcode.com` and new platform/user-agent.

```
```
