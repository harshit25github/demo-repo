````markdown
Fix Flight Agent `suggestedQuestions` issue in Extractor Agent.

Problem:
For Flight Agent, “You might ask” still shows invalid suggestions like:
- "Add origin city"
- "Add departure city"
- "Add travel city"
- "What travel dates?"
- "Pick a destination"

This is wrong because Flight Agent must not suggest questions where the agent chooses/provides:
- origin
- destination
- travel date/date range

These values must come from the user only.

## How to trigger workflow

[ADD WORKFLOW TRIGGER STEPS HERE]

## Testing rule

Do not provide all 3 mandatory fields together:
- origin
- destination
- travel date/date range

Reason:
Providing all 3 may trigger actual flight search, which is not required for this test.

Test only partial context:
- origin only
- destination only
- date only
- origin + date
- destination + date
- origin + destination

Goal is only to verify `suggestedQuestions`.

## Required fix

1. Ensure Extractor Agent receives:
```ts
currentAgent
searchKey
````

2. If `currentAgent` is Flight Agent:

* Remove/block suggestions related to origin, destination, or travel dates.
* Do not ask the agent to pick/suggest these values.

3. If `currentAgent` is Flight Agent and `searchKey` is missing:

* Do not show advanced filter/result suggestions like:

  * "Apply non-stop filter"
  * "Show morning departures"
  * "Show checked baggage flights"
  * "Compare current options"
  * "Which flight is best?"

4. Add a code-level sanitizer after Extractor Agent output:

* Remove blocked Flight Agent suggestions.
* Remove advanced suggestions when `searchKey` is missing.
* Remove duplicates/stale suggestions.

## Allowed safe suggestions without `searchKey`

* "Update cabin to economy"
* "Update cabin to premium economy"
* "Change trip type to round trip"
* "Add 2 adults"
* "Change passenger count"

## Tests

* Flight Agent + origin only → no destination/date suggestions.
* Flight Agent + destination only → no origin/date suggestions.
* Flight Agent + date only → no origin/destination suggestions.
* Flight Agent + no `searchKey` → no advanced filter suggestions.
* Old summary has bad suggestions → final output should remove them.

## Deliverable

* Recreate issue using partial-context tests.
* Fix prompt/logic with minimal changes.
* Add sanitizer.
* Add tests.
* Share root cause and final test results.

```
```
