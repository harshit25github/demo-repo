```markdown id="dynamic-prompt-searchcriteria-debug"
Debug Flight Agent dynamic prompt hydration issue.

Problem:
`searchCriteria` is correctly updating Flight context, but the updated values are not appearing in the Flight Agent dynamic prompt.

Scope:
- searchCriteria → Flight context mapping
- Flight context hydration
- dynamic prompt builder

Check:
1. Is updated `searchCriteria` correctly mapped into `context.flight`?
2. Is dynamic prompt using latest `context.flight` or stale context?
3. Is any old context overwriting updated values before prompt creation?
4. Are route/date/passenger/trip fields missing only in prompt or also in context?
5. Which function builds the Flight Agent dynamic prompt?

Add focused logs:
- incoming `searchCriteria`
- mapped `context.flight`
- context used before dynamic prompt
- final dynamic prompt hydrated values

Expected:
Flight Agent dynamic prompt should always use the latest updated Flight context derived from request `searchCriteria`.

Deliverable:
- Identify root cause.
- Fix hydration issue.
- Share files changed.
- Add before/after logs.
```
