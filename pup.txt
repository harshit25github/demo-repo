 const now = new Date();
  const pad = n => String(n).padStart(2, '0');

  const formattedDateTime = [
    now.getFullYear(),
    pad(now.getMonth() + 1),
    pad(now.getDate()),
    pad(now.getHours()),
    pad(now.getMinutes()),
    pad(now.getSeconds())
  ].join('___');

  const safeRecipient = recipient.replace(/[@.]/g, '_');
  const fileName = `${safeRecipient}_${formattedDateTime}.xlsx`;
  const filePath = path.join('./reports', fileName);
