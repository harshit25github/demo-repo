```markdown
Update listing page API domains from `www.cheapo.com` to `m.cheapo.com`.

Scope:
- Check the shared listing page files.
- Check listing API integrations.
- Check related tools/helpers where hardcoded listing URLs are used.

Required change:
- Replace all listing-related API/domain usage from:
  - `www.cheapo.com`
- To:
  - `m.cheapo.com`

Important:
- Update only listing page / listing API related URLs.
- Do not change unrelated domains or non-listing APIs.
- Avoid hardcoding if there is already a config/env-based domain setup.
- Prefer using a shared config constant if possible.

Please verify:
- listing API calls use the new mobile domain
- tool URLs use the new mobile domain
- no old `www.cheapo.com` listing URL remains
- existing API paths/query params remain unchanged

Deliverable:
- Update the domain.
- Share files changed.
- Confirm old vs new URL examples.
- Run basic tests to ensure listing APIs still work.
```
