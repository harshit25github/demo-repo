Hey, the current code is working but it looks a bit messy and hard to maintain.

Could you please refactor it with these points in mind:

* Keep the logic clean and readable
* Break large functions into smaller reusable functions
* Use meaningful variable and function names
* Remove duplicate or unused code
* Add comments only where the logic is not obvious
* Keep error handling clear
* Avoid changing the existing behavior unless required
* Make sure the final code is easy to debug and extend

Please also share a short summary of what you changed after refactoring.
