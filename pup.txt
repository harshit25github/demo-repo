Profile the airline retrieval flow and find the main latency bottlenecks, especially for slower airlines.

Check for:
- Slow/heavy DB queries
- N+1 patterns
- Missing indexes
- Redundant loops or repeated work
- Airline-specific slow paths
- Work that can be safely parallelized

Add lightweight timing logs per airline, query, and retrieval step.

Then optimize with batching, indexing, caching, loop cleanup, and safe parallelization. Keep API behavior and response shape unchanged.

Deliver:
- Slowest airlines + bottleneck summary
- Code/index changes
- Before/after latency results
- Risks and follow-ups

Goal: make retrieval fast, smooth, and scalable.
