The current 400 errors are happening because incomplete search criteria is getting initialized and sent back in the request flow.

As per the expected flow, search criteria should be initialized only when it comes from the app/listing page, because that contains complete search details. From the AI side, the search criteria can be partial during the initial conversation, as the user may not have provided all required details yet.

It looks like there may have been a recent change where the chat layer is also initializing the partial search criteria received from AI. Because of this, the same incomplete criteria is coming back to us and failing the request body schema validation.

Can we please check this flow and ensure initialization happens only for app-provided search criteria, not for partial AI-generated criteria?
