You’re working in a separate codebase where we need to integrate an already-built MCP server.

Context:
- The MCP server is already developed and working.
- It uses our existing retrieval tool.
- We have already tested retrieval through:
  1. Direct Express routes
  2. OpenAI client connected to the MCP server
- Both flows are working correctly.

Your task:
Integrate the existing MCP server connection into this current codebase.

I will provide:
- The relevant Express routes
- The MCP server details
- Example OpenAI SDK client code showing how to connect to the MCP server
- Any required env vars/config

Please do the following:

1. Review the provided OpenAI client example.
2. Understand how it connects to the MCP server.
3. Reuse the same MCP connection pattern in this codebase.
4. Wire the current app/routes so they can call the MCP-powered OpenAI client.
5. Keep the integration clean and consistent with the existing project structure.
6. Make sure env vars, URLs, ports, and config values are not hardcoded.
7. Add clear error handling and useful logs around:
   - OpenAI client initialization
   - MCP server connection
   - tool call execution
   - response handling

Validation:
- Start the current codebase.
- Confirm it connects to the MCP server successfully.
- Hit the provided route(s).
- Verify the request reaches the OpenAI client.
- Verify the OpenAI client calls the MCP server.
- Verify the MCP server returns the expected retrieval/tool response.
- Confirm the final response shape matches what the current codebase expects.

Do not rebuild the MCP server or retrieval logic.
Do not change the existing retrieval implementation unless required for integration.
The goal is only to connect this codebase to the already-working MCP server using the provided OpenAI SDK example.

Deliver:
- Files changed
- Env vars added/required
- How to run locally
- Test route(s) used
- Any issues or assumptions
