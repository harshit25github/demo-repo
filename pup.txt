Test both retrieval flows:

1. Direct Express injection/retrieval routes
2. New MCP server using the same retrieval tool via the OpenAI client

Run the servers locally, then compare both paths using the same known test queries.

Check:

- Express server starts cleanly
- MCP server starts cleanly
- OpenAI client connects to MCP correctly
- MCP calls the same underlying retrieval tool
- Direct route and MCP retrieval return the same or equivalent chunks
- topK, filters, namespace, metadata, document IDs, scores, and chunk IDs are preserved
- No chunks are missing, reordered unexpectedly, truncated, or renamed
- Payloads are not being reshaped incorrectly between OpenAI → MCP → retrieval tool
- Final MCP answers are grounded in the retrieved chunks

Test flow:

1. Start Express server and MCP server.
2. Hit the direct retrieval route with 3–5 known queries.
3. Run the same queries through the OpenAI client using MCP.
4. Compare logs for both paths:
   - incoming payload
   - retrieval tool input
   - retrieval tool output
   - final response
5. Identify any mismatch in chunks, scores, metadata, or final answer quality.
6. Fix anything that breaks parity.
7. Add/update tests if useful.

Deliver a short report:

- Commands used
- Queries tested
- Direct route results
- MCP results
- Differences found
- Fixes made
- Remaining issues

Goal: MCP retrieval should match the original Express route retrieval in accuracy, payload shape, and completeness.
