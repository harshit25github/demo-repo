I need you to create a NEW standalone service inside the existing codebase, but in a completely separate folder.

Very important constraints:
1. Do NOT modify the existing business logic.
2. Do NOT break or refactor the current working indexing, retrieval, agent, or tool flow.
3. Reuse the existing indexing and retrieval code wherever possible.
4. The goal is to expose the existing capabilities as REST APIs through a new service layer.
5. The new service must be isolated, clean, production-ready, and easy to maintain.

What I want built:
Create a new service in a separate folder that exposes APIs for:
- Indexing
- Retrieval

Also add:
- Swagger / OpenAPI documentation
- Clear request/response schemas
- Proper error handling
- Health check endpoint

Retrieval flow must look like this:
User request
-> API
-> OpenAI SDK agent
-> retrieval tool
-> grounded answer

Important note:
The agent should use the existing retrieval tool / retrieval logic already present in the codebase.
Do not rewrite the retrieval business logic unless absolutely required for service wiring.
The service layer should mainly act as an API wrapper/orchestration layer around the already working implementation.

Indexing flow expectation:
Input payload
-> API
-> existing indexing pipeline
-> indexing execution/result response

Implementation expectations:
1. Create a new folder for this service, for example:
   /services/retrieval-api
   or another suitable isolated folder structure.

2. Keep the architecture modular. Suggested layers:
   - routes / controllers
   - service layer
   - adapter/wrapper around existing indexing logic
   - adapter/wrapper around existing retrieval + agent logic
   - swagger config
   - validation schemas
   - config / env handling

3. The API service should not duplicate core business logic if existing modules can be imported and reused.

4. Expose REST endpoints such as:
   - POST /index
   - POST /retrieve
   - GET /health
   - GET /docs (or Swagger UI route)

5. Retrieval endpoint behavior:
   - Accept user query and any required context/config
   - Invoke OpenAI SDK agent
   - Agent must call the retrieval tool
   - Return grounded answer
   - Return structured response with answer, citations/sources if available, and status metadata if supported by current code

6. Indexing endpoint behavior:
   - Accept indexing input in a clean API contract
   - Trigger existing indexing flow
   - Return success/failure details and any relevant indexing metadata

7. Swagger / OpenAPI:
   - Document every endpoint
   - Include example request and response payloads
   - Make the docs usable through Swagger UI

8. Non-functional requirements:
   - No changes to existing behavior
   - No unnecessary refactor of legacy/current modules
   - Clean logging
   - Robust error handling
   - Environment-based configuration
   - Easy local run instructions

9. Deliverables expected from you:
   - New service folder with complete implementation
   - README for how to run it
   - API documentation through Swagger
   - Clear explanation of how existing indexing/retrieval code was reused
   - Mention any assumptions made
   - Mention if any minimal changes outside the new folder were unavoidable, and why

10. Acceptance criteria:
   - Existing code continues to work unchanged
   - New service runs independently
   - Indexing API works using existing indexing logic
   - Retrieval API works using this exact flow:
     API -> OpenAI SDK agent -> retrieval tool -> grounded answer
   - Swagger documentation is accessible
   - Code is structured and maintainable

Before implementing:
- First inspect the current codebase and identify the existing indexing modules, retrieval modules, agent setup, and retrieval tool integration points.
- Then design the new service around those modules with minimum intrusion.
- Prefer adaptation/wrapping over rewriting.

Important:
This is a service extraction / API exposure task, not a business logic rewrite task.
Preserve current behavior as much as possible.
