We have a stale search criteria issue in the chat flow. When the user updates flight details through chat, the next request should not send the original listing-page search criteria again, otherwise our AI service may apply filters/search on outdated data.

Suggested solution: Chat service should treat the first listing-page payload as bootstrap context only, and after every AI response, persist/echo back the latest updated search criteria + searchKey/sid for all subsequent chat requests.
