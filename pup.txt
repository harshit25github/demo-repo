````markdown
Fix Flight Agent `suggestedQuestions` ownership with Summary Extractor.

Current misunderstanding:
We do NOT need Summary Extractor to handle Flight Agent suggestion rules anymore.

Do not add Flight-specific prompt rules, fallback logic, sanitizer, or blocked-question logic inside Summary Extractor.

Required flow:

1. Main agent workflow runs.
2. If Flight Agent is triggered, Flight Agent generates its own suggested questions and stores them in:

```ts
context.flight.suggestedQuestions
````

3. After workflow completes, Summary Extractor should still run normally with:

* current user message
* current agent response
* older summary context

4. Let Summary Extractor generate/update summary as-is.
   It can generate its own `suggestedQuestions`; do not restrict it inside the extractor.

5. After Summary Extractor returns updated summary:
   If `lastAgent` is Flight Agent, overwrite final summary suggested questions with:

```ts
summaryContext.suggestedQuestions = context.flight.suggestedQuestions
```

That’s it.

Important:

* Remove Flight-specific fallback/sanitizer logic from Summary Extractor.
* Remove Flight-specific “do not generate origin/destination/date questions” handling from Summary Extractor.
* Do not make Summary Extractor responsible for Flight suggestions.
* Flight Agent owns Flight suggested questions.
* Summary Extractor only updates summary context.
* Final replacement should happen after extractor output is received.

Expected behavior:

* For Flight Agent: final `suggestedQuestions` always come from `context.flight.suggestedQuestions`.
* For non-Flight agents: keep existing Summary Extractor behavior unchanged.

Deliverable:

* Update integration flow only.
* Remove old Flight-specific extractor suggestion handling.
* Confirm final summary uses Flight Agent suggestions when `lastAgent` is Flight Agent.
* Add a small test showing extractor-generated questions are replaced by `context.flight.suggestedQuestions`.

```
```
