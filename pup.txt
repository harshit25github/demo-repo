You are integrating an existing retrieval workflow into an existing agentic backend.

Important constraints:
- Do not redesign crawling or indexing.
- Crawling and indexing are already complete and should be treated as existing infrastructure.
- Your task is only to understand and integrate the **primary user-facing agent** and its **retrieval-tool workflow**.

## What already exists

There is already a retrieval pipeline that:
- accepts a user query
- performs retrieval against an indexed knowledge base
- returns grounded chunks/results
- is already wired to support HyDE internally
- is already capable of handling both:
  - semantic chunk retrieval for policy-style pages
  - row-first retrieval for the CheapOair baggage directory

You should not change that retrieval logic unless integration requires a very small adaptation.

## What you need to understand

The system has a **primary OpenAI SDK agent** that interacts with the user.

That agent:
- is the user-facing agent
- does **not** answer directly from memory
- uses a function tool called `retrieve_context`
- the tool calls the existing retrieval service
- the retrieved results are returned to the agent
- the agent then writes the final grounded answer with citations

This is not a multi-agent system.
It is a **single primary agent** with a **single retrieval tool**.

## Core flow

The exact flow is:

```text
User query
-> primary OpenAI SDK agent
-> retrieve_context tool call
-> retrieval service
-> relevant chunks/results returned
-> agent reads tool output
-> agent writes final grounded answer
