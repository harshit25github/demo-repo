````markdown
Fix ONLY the Extractor Agent `suggestedQuestions` logic. Do not modify any other agent/code area.

Strict scope:
- Work only on Extractor Agent related files.
- You may inspect other agents only to understand data flow.
- Do NOT update Flight Agent prompt.
- Do NOT update Trip Planner Agent.
- Do NOT update Policy Agent.
- Do NOT update Gateway routing logic.
- Do NOT update React/UI/client code.
- Do NOT refactor unrelated shared code.
- Do NOT change source code outside Extractor Agent unless absolutely required, and if required, ask/mention first before changing.

Problem:
Extractor Agent generates `suggestedQuestions` shown under “You might ask”.

Current issues:
1. It suggests invalid Flight Agent questions like:
   - "Add origin city"
   - "Add departure city"
   - "Pick a destination"
   - "What travel dates?"

2. It sometimes suggests advanced flight filters before search exists:
   - "Apply non-stop filter"
   - "Show morning departures"
   - "Compare current options"

3. After blocking bad suggestions, sometimes `suggestedQuestions` becomes empty.

Required fix inside Extractor Agent only:

Input expected:
```ts
{
  oldSummaryContext,
  currentUserMessage,
  currentAgentResponse,
  currentAgent,
  searchKey
}
````

Rules:

If `currentAgent` is Flight Agent:

1. Never suggest questions where agent chooses/provides:

* origin
* destination
* travel date/date range

These values must come from the user only.

2. If `searchKey` is missing:
   Do not suggest advanced filters/result questions.

Blocked without `searchKey`:

* "Apply non-stop filter"
* "Show morning departures"
* "Show checked baggage flights"
* "Compare current options"
* "Which flight is best?"

3. `suggestedQuestions` should never be empty.
   If sanitizer removes all suggestions, add safe fallback suggestions like:

* "Update cabin to economy"
* "Update cabin to premium economy"
* "Change trip type to round trip"
* "Add 2 adults"
* "Change passenger count"

Implementation:

* Update Extractor Agent prompt/logic.
* Add deterministic sanitizer inside Extractor Agent flow.
* Remove duplicate/stale suggestions.
* Add fallback suggestions if final array becomes empty.
* Keep output as `suggestedQuestions: string[]`.

Testing rule:
Do not trigger full flight search while testing.
Do not provide all 3 together:

* origin
* destination
* travel date

Use partial-context tests only:

* origin only
* destination only
* date only
* origin + date
* destination + date
* origin + destination

Deliverable:

* Fix only Extractor Agent logic.
* Show files changed.
* Confirm no other agent/UI/source area was modified.
* Add tests for invalid suggestions, advanced suggestions without `searchKey`, duplicate suggestions, and empty suggestions.
* Provide root cause and final test results.

```
```
