```markdown
Add logging and pytest coverage for `price_prediction_tool`.

Scope:
- Work only on `price_prediction_tool` and its tests.
- Do not modify unrelated Flight Agent logic unless required.

Task:
Add focused debug logging inside `price_prediction_tool` so we can easily debug failures.

Log only important checkpoints:
- received input payload
- validated/normalized input
- generated ClickHouse query params
- ClickHouse response summary
- parsed thresholds/predictions summary
- final output status
- error details if any

Do not log huge raw responses or sensitive data.

## How to test

I will provide the exact test steps/commands here:

[ADD TEST STEPS HERE]

## Pytest requirement

Add pytest coverage for 4–5 scenarios:

1. Valid one-way prediction
2. Valid round-trip prediction
3. Missing required fields
4. Date outside supported range
5. Unsupported route / no predictions

For each test, verify:
- expected status
- expected output shape
- logs are meaningful
- no raw/internal error is exposed

Deliverable:
- Add logging.
- Add pytest tests.
- Share sample logs.
- Share test results.
```
