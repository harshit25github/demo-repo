"Hey Opus, let's update the architecture. Previously, I was only handling conversations using OpenAI’s SDK. Each chat had a chat ID and a conversation ID, and I managed sessions based on that. But now I need to handle the chat IDs and messages more directly in my service.

In this updated architecture, if a chat ID is null, that means it's a new chat, so we create a new chat record in the database and generate a new UUID for it. If the chat ID exists, we continue the conversation. We'll also store messages in a message table.

So please help me write the code that:

Checks if the chat ID is null and, if so, creates a new chat entry in the database.

Retrieves or creates the appropriate conversation ID.

Stores incoming messages in the message table linked to that chat.

Continues the chat flow and returns responses as before, but now with the new database-backed structure."
