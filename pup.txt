For the Human Handoff Request Guardrail, tripwire happens only on the second occurrence. Until then, the summary is updated normally.

On tripwire, we do not update the summary because the workflow exits. So the consumer can use the latest summary from the previous successful turn, i.e. the N-1 summary.

If needed, they can also fetch it via /chat/{chatId} API.
