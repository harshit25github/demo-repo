Fix the OpenAI fine-tuned model issue.

Context:

* Fine-tuned model worked with Agents SDK for ~5 months.
* Now it fails with `404 model_not_found` on Responses API / Agents SDK.
* Same model still works with Chat Completions.
* Direct `client.responses.create({ model: "ft:...", input: "test" })` also fails.

Please implement:

1. Use one shared OpenAI client config everywhere: `apiKey`, `organization`, `project`, `baseURL`.
2. Add a check: try Responses first, and if it returns `404 model_not_found`, fallback to Chat Completions.
3. For Agents SDK, use the shared client and switch to Chat Completions when fallback is needed:

```ts
setDefaultOpenAIClient(client);
setOpenAIAPI("chat_completions");
```

4. Do not silently move hosted-tool flows to Chat Completions. If tools are needed, split the flow: Responses for tools, Chat Completions for the fine-tuned model.
5. Log the model ID, endpoint used, full error, and OpenAI request ID.

Expected result: production works again, and we have enough logs to confirm whether this is an OpenAI-side Responses routing issue.

