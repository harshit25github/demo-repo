````markdown id="abort-timeout-crisp"
Analyze AbortController timeout behavior in our streaming API.

Do not implement yet. Just explain current behavior and recommended fix.

Current flow:
```txt
client/service → our API → OpenAI streaming API → chunks → our API forwards chunks to client
````

Questions to clarify:

1. When does the 2-minute timeout trigger?

* from request start?
* after no chunks/inactivity?
* based on current implementation?

2. What happens after timeout?

* which event/path is hit: error, close, abort, catch?
* does stream reach normal end/done?

3. Client notification

* if chunks already started, can we send timeout chunk?
* should we send final chunk like:

```json
{
  "status": "timeout",
  "message": "Request timed out before completion."
}
```

* should we call `res.end()` after this?
* how to avoid duplicate final chunks?

4. Cleanup
   Verify:

* OpenAI stream aborted
* timer cleared
* response closed safely
* no hanging connection
* no duplicate final response

5. Logs needed
   Check/add logs for:

* request started
* timeout started
* timeout triggered
* abort called
* timeout chunk sent
* response ended
* normal completion
* abrupt close

Deliverable:

* Explain current timeout behavior.
* Show exact error/event path.
* Confirm whether client currently gets notified.
* Recommend safe handling flow.
* List files/functions involved.

```
```
