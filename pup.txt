Hey! I need a fix for the response formatting. Right now, the agent adds numbered annotations (like "1", "2", "3") in the text, but only one link actually appears, making it super confusing. So here's what I want:

Drop the numbered annotations entirely from the main text. No "1", "2", "3" inside the response text itself.
Instead, if a source link is needed, add it at the end, after a clean spacing gap, like a footer.
Make sure those links don’t break—give them proper formatting and spacing so they appear as full, clickable URLs without splitting.

In short: No inline numbering. Just clear text. Then, at the end, neatly formatted links if needed.
